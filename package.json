{
  "name": "jan-shield-ai",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "py -m backend.app",
    "dev": "py -m backend.app",
    "seed": "py -m backend.seed",
    "test": "py -m unittest discover -s backend/tests -p 'test_*.py' -v"
  }
}
